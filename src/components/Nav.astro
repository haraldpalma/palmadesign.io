---
import Logo from './Logo.astro';
import { NAV, SITE_NAME, brand } from '../lib/site';

// Active state from the current path (section pages + their children).
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) => path === href || path.startsWith(href + '/');
---
<nav class="nav" data-sec="dark">
  <div class="wrap-wide nav-inner">
    <a href="/" class="nav-logo" aria-label={`${SITE_NAME} — home`}>
      <Logo variant="wordmark" height={26} />
    </a>

    <div class="nav-links">
      {
        NAV.map((item) => (
          <a href={item.href} class="nav-link" aria-current={isActive(item.href) ? 'page' : undefined}>
            {item.label}
          </a>
        ))
      }
    </div>

    <button type="button" class="theme-toggle" data-theme-toggle aria-label="Toggle light / dark theme">
      <svg class="ic ic-moon" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z"/></svg>
      <svg class="ic ic-sun" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4.2"/><path d="M12 2v2.2M12 19.8V22M4.2 4.2l1.6 1.6M18.2 18.2l1.6 1.6M2 12h2.2M19.8 12H22M4.2 19.8l1.6-1.6M18.2 5.8l1.6-1.6"/></svg>
    </button>

    <a href="/contact" class="nav-cta">{brand.cta}</a>

    <button type="button" class="nav-burger" data-menu-open aria-label="Open menu" aria-expanded="false">
      <span></span>
      <span></span>
    </button>
  </div>
</nav>

<div class="nav-drawer" data-sec="dark" data-drawer aria-hidden="true">
  <div class="nav-drawer-top">
    <Logo variant="mark" height={26} />
    <button type="button" class="nav-drawer-close" data-menu-close aria-label="Close menu">×</button>
  </div>
  <div class="nav-drawer-links">
    {NAV.map((item) => <a href={item.href}>{item.label}</a>)}
    <a href="/contact">Contact</a>
  </div>
  <div class="nav-drawer-foot">
    <a class="nav-cta" href="/contact" data-menu-close>{brand.cta}</a>
    <button type="button" class="theme-toggle" data-theme-toggle aria-label="Toggle light / dark theme">
      <svg class="ic ic-moon" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.8A9 9 0 1 1 11.2 3 7 7 0 0 0 21 12.8z"/></svg>
      <svg class="ic ic-sun" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4.2"/><path d="M12 2v2.2M12 19.8V22M4.2 4.2l1.6 1.6M18.2 18.2l1.6 1.6M2 12h2.2M19.8 12H22M4.2 19.8l1.6-1.6M18.2 5.8l1.6-1.6"/></svg>
    </button>
  </div>
</div>

<script>
  // --- Light / dark theme -------------------------------------------------
  const root = document.documentElement;
  function setTheme(t: string) {
    root.setAttribute('data-theme', t);
    try {
      localStorage.setItem('pd-theme', t);
    } catch {}
  }
  document.querySelectorAll('[data-theme-toggle]').forEach((btn) =>
    btn.addEventListener('click', () =>
      setTheme(root.getAttribute('data-theme') === 'dark' ? 'light' : 'dark')
    )
  );

  // --- Mobile drawer ------------------------------------------------------
  const drawer = document.querySelector('[data-drawer]');
  const openBtn = document.querySelector('[data-menu-open]');
  function setMenu(open: boolean) {
    if (!drawer) return;
    drawer.setAttribute('data-open', String(open));
    drawer.setAttribute('aria-hidden', String(!open));
    openBtn?.setAttribute('aria-expanded', String(open));
    document.body.style.overflow = open ? 'hidden' : '';
  }
  openBtn?.addEventListener('click', () => setMenu(true));
  document.querySelectorAll('[data-menu-close]').forEach((b) =>
    b.addEventListener('click', () => setMenu(false))
  );
  drawer?.querySelectorAll('a').forEach((a) => a.addEventListener('click', () => setMenu(false)));
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') setMenu(false);
  });
</script>
